import 'package:flutter/material.dart';

class AddTherapyScreen extends StatelessWidget {
  const AddTherapyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Aggiungi ospedale'),
      ),
      body: const Center(),
    );
  }
}
