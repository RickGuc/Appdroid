import 'package:flutter/material.dart';

class AddEsenzioniScreen extends StatelessWidget {
  const AddEsenzioniScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Aggiungi ospedale'),
      )
    );
  }
}
